<div class="btn-toolbar">
    <div class="btn-group">
        <button class="btn dropdown-toggle" data-toggle="dropdown">
            <i class="icon-plus"></i>
            New
            <span class="caret"></span>
        </button>
        <ul class="dropdown-menu">
            {% for provider in bl_row_providers() %}
                <li><a href="{{ path('bl_backlogrow_new', {backlog_uid: backlog.uid, type: provider.name}) }}">
                    {{ provider.label }}
                </a></li>
            {% endfor %}
        </ul>
    </div>
        <div class="btn-group">
        <button class="btn dropdown-toggle" data-toggle="dropdown">
            <i class="icon-signal"></i>
            Options
            <span class="caret"></span>
        </button>
        <ul class="dropdown-menu">
            <li><a href="#">Burn chart</a></li>
            <li><a href="#">Statistics</a></li>
            <li class="divider"></li>
            <li><a href="{{ path('bl_backlog_edit', {uid: backlog.uid }) }}">Options</a></li>
        </ul>
    </div>
</div>
